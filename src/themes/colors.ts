export interface Colors {
    background: {
        primary: string,
        pageBackground: string,
    },
    texting: {
        sideBarLabel: string
    }
}

const colors = {
    background: {
        primary: '#282828',
        pageBackground: "#f0f2f5"
    },
    texting: {
        sideBarLabel: '#5f6368'
    }
}

export default colors;