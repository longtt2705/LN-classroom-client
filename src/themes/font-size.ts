export interface FontSizes {
    default: number,
    sizeLabel:number,
    changePass:number,
    codeclass:number
}

const fontSizes:FontSizes = {
    default: 14,
    sizeLabel:25,
    changePass:18,
    codeclass:20
}

export default fontSizes;