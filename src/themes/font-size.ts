export interface FontSizes {
    default: number,
    sizeLabel:number,
    changePass:number,
}

const fontSizes: FontSizes = {
    default: 14,
    sizeLabel:25,
    changePass:18
}

export default fontSizes;